# Description: A system for managing library compile/link flags
# URL: http://pkgconfig.freedesktop.org/wiki/
# Maintainer: CRUX System Team, core-ports at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au

name=pkg-config-compat32
version=0.23
release=1
source=(http://$name.freedesktop.org/releases/pkg-config-$version.tar.gz)

build() {
	cd pkg-config-$version
	./configure --prefix=/usr --libdir=/usr/lib32
	make
	make DESTDIR=$PKG install
	rm -rf $PKG/usr/{man,share}
	mv $PKG/usr/bin/pkg-config $PKG/usr/bin/pkg-config32
}