# Description: Library giving userland programs access to USB devices
# URL:         http://libusb.sourceforge.net/
# Maintainer:  CRUX System Team, core-ports at crux dot nu
# Arch Maintainer: Lucas Hazel, lucas at die dot net dot au

name=libusb
version=1.0.1
release=1
source=(http://dl.sourceforge.net/sourceforge/$name/$name-$version.tar.bz2)

build () {
    cd $name-$version
    
    ./configure --prefix=/usr 
    make
    make DESTDIR=$PKG install
    
    install -d $PKG/lib
    mv $PKG/usr/lib/libusb-1.0.so* $PKG/lib
    ln -s /lib/libusb-1.0.so $PKG/usr/lib/libusb-1.0.so
}
